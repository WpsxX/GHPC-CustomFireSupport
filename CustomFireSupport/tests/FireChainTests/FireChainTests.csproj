<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <!-- Only the game-free rules are compiled in: they are the two decisions the CAS firing-chain
         repair is built on, and both are transcribed from the shipped assembly. -->
    <Compile Include="../../CasFireChainRules.cs" Link="CasFireChainRules.cs" />
  </ItemGroup>
</Project>
